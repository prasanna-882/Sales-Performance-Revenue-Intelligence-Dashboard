# 📊 DAX KPI README – Sales Performance Dashboard

This document explains all the **DAX measures (KPIs)** used in the Sales Performance Power BI report. Each KPI includes its **purpose**, **business meaning**, and **DAX logic explanation** in easy English.

---

## 🔹 1. Total Revenue

**Definition:** Total money earned from all sales.

**DAX:**

```DAX
Total Revenue = 
SUM ( fact_sales[revenue] )
```

**Why it matters:** Core business performance indicator.

---

## 🔹 2. Total Orders

**Definition:** Total number of orders placed.

**DAX:**

```DAX
Total Orders = 
COUNT ( fact_sales[sales_id] )
```

---

## 🔹 3. Total Quantity

**Definition:** Total number of products sold.

**DAX:**

```DAX
Total Quantity = 
SUM ( fact_sales[quantity] )
```

---

## 🔹 4. Total Customers

**Definition:** Number of unique customers.

**DAX:**

```DAX
Total Customers = 
DISTINCTCOUNT ( fact_sales[customer_id] )
```

---

## 🔹 5. Average Order Value (AOV)

**Definition:** Average revenue earned per order.

**Formula:** Total Revenue ÷ Total Orders

**DAX:**

```DAX
Average Order Value = 
DIVIDE ( [Total Revenue], [Total Orders] )
```

---

## 🔹 6. Average Revenue per Customer

**Definition:** Average revenue generated from each customer.

**DAX:**

```DAX
Avg Revenue per Customer = 
DIVIDE ( [Total Revenue], [Total Customers] )
```

---

## 🔹 7. Average Customer Rating

**Definition:** Average rating given by customers.

**DAX:**

```DAX
Average Customer Rating = 
AVERAGE ( fact_sales[customer_rating] )
```

---

## 🔹 8. Average Delivery Days

**Definition:** Average number of days taken to deliver orders.

**DAX:**

```DAX
Average Delivery Days = 
AVERAGE ( fact_sales[delivery_days] )
```

---

## 🔹 9. Average Discount Percentage

**Definition:** Average discount offered across all orders.

**DAX:**

```DAX
Average Discount % = 
DIVIDE ( 
    AVERAGE ( fact_sales[discount_percent] ),
    100
)
```

**Note:** Divided by 100 to display as percentage.

---

## 🔹 10. Returned Orders

**Definition:** Total number of orders that were returned.

**DAX:**

```DAX
Returned Orders = 
CALCULATE ( 
    COUNT ( fact_sales[sales_id] ),
    fact_sales[is_returned] = 1
)
```

---

## 🔹 11. Return Rate %

**Definition:** Percentage of orders that were returned.

**Formula:** Returned Orders ÷ Total Orders

**DAX:**

```DAX
Return Rate % = 
DIVIDE ( [Returned Orders], [Total Orders] )
```

---

## 🔹 12. Repeat Customers

**Definition:** Customers who placed more than one order.

**DAX:**

```DAX
Repeat Customers = 
COUNTROWS (
    FILTER (
        VALUES ( fact_sales[customer_id] ),
        CALCULATE ( COUNT ( fact_sales[sales_id] ) ) > 1
    )
)
```

---

## 🔹 13. Revenue Growth % (YoY)

**Definition:** Year-over-Year revenue growth percentage.

**DAX:**

```DAX
Revenue Growth % = 
DIVIDE (
    [Total Revenue] - 
    CALCULATE ( [Total Revenue], DATEADD ( dim_date[order_date], -1, YEAR ) ),
    CALCULATE ( [Total Revenue], DATEADD ( dim_date[order_date], -1, YEAR ) )
)
```

---

## 🔹 14. Top Product Revenue

**Definition:** Revenue generated by the highest performing product category.

**DAX:**

```DAX
Top Product Revenue = 
MAXX (
    VALUES ( dim_product[product_category] ),
    [Total Revenue]
)
```

---

## 🔹 15. Average Revenue per Region

**Definition:** Average revenue across all regions.

**Assumption:** Total 6 regions

**DAX:**

```DAX
AVG Revenue per Region = 
[Total Revenue] / 6
```

---

## 🔹 16. Most Used Payment Method

**Definition:** Payment method with the highest number of orders.

**DAX:**

```DAX
Most Used Payment Method = 
VAR PaymentTable =
    SUMMARIZE (
        dim_payment,
        dim_payment[payment_method],
        "OrderCount", [Total Orders]
    )
RETURN
    MAXX (
        TOPN ( 1, PaymentTable, [OrderCount], DESC ),
        dim_payment[payment_method]
    )
```

---

## ✅ Summary

These KPIs help analyze:

* 📈 Revenue & growth performance
* 👥 Customer behavior
* 🚚 Delivery efficiency
* 🔁 Returns & repeat customers
* 💳 Payment preferences

This README can be used for **GitHub**, **project documentation**, or **interview explanation
